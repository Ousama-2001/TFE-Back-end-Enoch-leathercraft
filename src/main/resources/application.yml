server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enochdb
    username: postgres
    password: ouss
    driver-class-name: org.postgresql.Driver

  web:
    resources:
      static-locations: "classpath:/static/,file:./uploads/"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: false

  mail:
    host: smtp.gmail.com        # ou host Mailtrap / autre
    port: 587
    username: "saidenoch@gmail.com"          # <-- à remplacer
    password: "qexm wvao ianm splz" # <-- à remplacer (app password, pas le mdp normal)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs
    disable-swagger-default-url: true

app:
  jwt:
    secret: "change-me-to-a-256-bit-secret-at-least-32-bytes-long-1234567890"
    expiration-minutes: 120

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
