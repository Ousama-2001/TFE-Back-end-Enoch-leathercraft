server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enochdb
    username: postgres
    password: ouss
    driver-class-name: org.postgresql.Driver

  web:
    resources:
      static-locations: "classpath:/static/,file:./uploads/"

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: "saidenoch@gmail.com"
    password: ""  # ← IMPORTANT : vide ici
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  # ← ON AJOUTE CETTE PARTIE ICI
  config:
    import: optional:application-secret.yml

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs
    disable-swagger-default-url: true

app:
  jwt:
    secret: "change-me-to-a-256-bit-secret-at-least-32-bytes-long-1234567890"
    expiration-minutes: 120
    frontend-url: "http://localhost:4200"

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG

stripe:
  secret-key: ""  # ← IMPORTANT : vide ici
